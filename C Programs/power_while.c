#include<stdio.h>
void main(){
    int x,y,i=1,a=1;
    printf("Enter the base:");
    scanf("%d",&x);
     printf("Enter the base:");
    scanf("%d",&power);
    while(i<=y){
      a=a*x;
      i++;
    }printf("%d",a);
}